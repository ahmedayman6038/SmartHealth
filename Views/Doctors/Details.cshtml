@model IEnumerable<SmartHealth.Models.SpecialtyDoctor>

@{
    ViewData["Title"] = "Details";
}

<!-- Content Wrapper. Contains page content -->
<div class="content-wrapper">
    <!-- Content Header (Page header) -->
    <section class="content-header">
        <h1>
            Doctors
            <small><a asp-action="Index" asp-controller="Doctors">View List</a></small>
        </h1>
        <ol class="breadcrumb">
            <li><a href="#"><i class="fa fa-dashboard"></i> Doctors</a></li>
            <li class="active">@ViewData["Title"]</li>
        </ol>
    </section>

    <!-- Main content -->
    <section class="content container-fluid">

        <div class="row">
            <!-- left column -->
            <div class="col-md-12">
                <div class="box box-solid">
                    <div class="box-header with-border">
                        <i class="fa fa-text-width"></i>

                        <h3 class="box-title">Details Of Doctor</h3>
                    </div>
                    <!-- /.box-header -->
                    <div class="box-body">
                        <dl class="dl-horizontal">
                            <dt>ٍDoctor Name</dt>
                            <dd>@Model.First().Doctor.Name</dd>
                            <dt>ٍDoctor Email</dt>
                            <dd>@Model.First().Doctor.Email</dd>
                            <dt>ٍDoctor City</dt>
                            <dd>@Model.First().Doctor.City</dd>
                            <dt>ٍDoctor Address</dt>
                            <dd>@Model.First().Doctor.Address</dd>
                            <dt>ٍDoctor Created</dt>
                            <dd>@Model.First().Doctor.CreatedData.ToString("dd/MM/yyyy")</dd>
                            <dt>Doctor Specialties</dt>
                            <dd>
                                <ul>
                                    @foreach (var item in Model)
                                    {
                                        <li>@item.Specialty.Name</li>
                                    }
                                </ul>
                            </dd>
                        </dl>
                    </div>
                    <!-- /.box-body -->
                </div>
                <!-- /.box -->
            </div>
            <!--/.col (left) -->

        </div>
        <!-- /.row-->
    </section>
    <!-- /.content -->
</div>
<!-- /.content-wrapper -->